[package]
name = "automation-2040w"
version = "0.1.0"
edition = "2024"

[dependencies]
embassy-executor = { workspace = true }
embassy-rp = { version = "0.8.0", features = ["rp2040"] }
logic = { path = "../logic" }

[features]
default = []